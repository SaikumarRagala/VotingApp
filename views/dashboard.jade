extends layout

block content
  h1 Votes polled for each technology
  br
  br
  table.table
    thead
      tr
        th(scope='col') Index
        th(scope='col') Technology
        th(scope='col') Votes
    tbody
      tr
        th(scope='row') 1
        td Machine Learning
        td #{ML}
      tr
        th(scope='row') 2
        td AR
        td #{AR}
      tr
        th(scope='row') 3
        td Blockchain
        td #{Blockchain}
      tr
        th(scope='row') 4
        td Serverless
        td #{Serverless}
